#include <cstdio>
#include <cstring>
const int MAX_LEN = 50000;
int a[MAX_LEN+5];
int dp1[MAX_LEN+5];
int dp2[MAX_LEN+5];
int main()
{
	int cas;
	scanf("%d",&cas);
	while (cas--)
	{
		int n;
		scanf("%d",&n);
		for (int i=0;i<n;i++)
		  scanf("%d",&a[i]);
		memset(dp1,0,sizeof(dp1));
		memset(dp2,0,sizeof(dp2));
		for (int i=0;i<n;i++)
		{
			if (a[i]>0)
			  dp1[i] += a[i];
			else
		}
	}
	return 0;
}
