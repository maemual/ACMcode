#include <stdio.h>

int main()
{
  float n,len;
  while (1)
    {
      scanf("%f",&n);
      if (len == 0.00)
	break;
      n=1;
      while ((1.0 /(n+1))<len)
	n++;
      printf("%d card(s)\n",n);
    }
  return 0;
}
