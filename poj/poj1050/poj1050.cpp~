#include <cstdio>
const int MAX_LEN = 100;
int map[MAX_LEN+5][MAX_LEN+5];
int main()
{
	int n;
	while (scanf("%d",&n) != EOF)
	{
		for (int i=0;i<n;i++)
		  for (int j=0;j<n;j++)
			scanf("%d",&map[i][j]);

	}
}
