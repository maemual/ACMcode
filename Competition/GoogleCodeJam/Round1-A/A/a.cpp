#include <stdio.h>

int main()
{
	int t;
	scanf("%d",&t);
	for (int k=1;k<=t;++k)
	{
		int typed,total;
		scanf("%d%d",&typed,&total);
		float prob[100000];
		for (int i = 0;i<typed;++i)
		  scanf("%f",&prob[i]);
		
		printf("Case #%d: ",k);


	}
}
