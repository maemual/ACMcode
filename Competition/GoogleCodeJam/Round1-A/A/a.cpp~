#include <stdio.h>

int main()
{
	int t;
	scanf("%d",&t);
	while (t--)
	{
		int typed,total;
		scanf("%d%d",&typed,&total);
		float prob[100000];
		for (int i = 0;i<typed;++i)
		  scanf("%f",&prob[i]);
		for (int i=0;i<typed;++i)
		  printf("%f ",prob[i]);
	}
}
