#include <cstdio>
#include <cstring>
const int MAX_LEN = 10000;
char s[MAX_LEN+5];
char t[MAX_LEN+5];
int send,tend;
int len1,len2;
int main()
{
	int cas;
	scanf("%d",&cas);
	while (cas--)
	{
		scanf("%s",s);
		scanf("%s",t);
		len1 = strlen(s);
		len2 = strlen(t);
		if (len1 % 2 == 1)
		{
			bool flag = false;
			for (int i=0;i<len1-1;i++)
			{
				if (s[i] != s[i+1])
				{
					flag = true;
					break;
				}
			}
			if (flag)
			  send = 1;
			else 
			  send = len1-1;
		}
		for (int i=1;i<=len/2;i++)
		{
			if (len % i == 0)
			{
				bool flag = false;
				for (int j=1;j<=len/i-1;j++)
				{
					if (cmp(0,j*i,))
				}
			}
		}
	}
	return 0;
}
